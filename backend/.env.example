# ── Servidor ────────────────────────────────
PORT=3000

# ── Banco de Dados MySQL ─────────────────────
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=sua_senha_aqui
DB_NAME=greenlife

# ── JWT ──────────────────────────────────────
JWT_SECRET=greenlife_super_secret_2024_mude_isso_em_producao

# ── CORS (URL do frontend, se separado) ──────
FRONTEND_URL=http://localhost:3000
